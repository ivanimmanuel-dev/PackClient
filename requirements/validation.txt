# Optional validation engines; the Python decoder itself uses only the standard library.
# Versions match the previously verified CI configuration.
yara-python==4.5.4
pysigma==1.5.0
pysigma-backend-sqlite==1.2.4
lz4==4.4.5
